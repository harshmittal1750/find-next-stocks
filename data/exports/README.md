# Generated exports

Parquet snapshots are written here and ignored by Git. The pipeline can reproduce them. CSV is not
used as an application storage format; migrated CSV sources live in PostgreSQL's `archive` schema.
